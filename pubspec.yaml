name: oa_cep
description: Package consulta Cep app OdontoAtual.
version: 1.0.0
homepage: https://github.com/fcraveiro/oa_cep

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ">=4.0.4 <5.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
